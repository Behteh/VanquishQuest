version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - java -version
      - mvn install
artifacts:
  files:
   - '**/Vanquishquest.war'
   - 'appspec.yaml'
   discard-paths: yes
